/**
 * Account Model Module
 * Feature-Sliced Design: features/account/model
 */

export type { UserProfile, UpdateProfileData, Gender } from './types';
export { ProfileProvider, useProfile } from './ProfileContext';
